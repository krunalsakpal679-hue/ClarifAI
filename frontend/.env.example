# ClarifAI Frontend Environment Variables
# Build-time API Base URL (connects to Django API backend at port 8000)
VITE_API_BASE_URL=http://localhost:8000

# Set to false to connect directly to the live Django backend API
# In production builds, mocks are disabled by default (fails closed to real services)
VITE_USE_MOCKS=false
